# RGBID + F4 3:1 Ensemble 线上 A/B 验证记录

**日期**：2026-09-17
**比赛截止**：2026-09-20 20:00
**状态**：**提交未执行（无可用提交通道）** → 线上成绩 `PENDING`

---

## 0. 执行摘要

| 阶段 | 状态 |
|---|---|
| 阶段 1 · 提交前检查（12 项） | ✅ **已完成**，11 项 PASS / 1 项 FAIL（空文件，见 §1.6） |
| 阶段 2 · 正式保底保护核验 | ✅ **已完成**，6 项冻结产物全部未变动 |
| 阶段 3 · 执行线上提交 | ❌ **未执行 —— 本机无提交通道** |
| 阶段 4 · 记录提交 | ✅ 已记录（本文件），成绩列 `PENDING` |
| 阶段 5 · 结果判定 | `ONLINE_SCORE_PENDING`（原因见 §3，**非"已提交待返回"**） |

---

## 1. 阶段 1：提交前检查结果

**候选包**：`submissions/rgbid_f4_ensemble_3to1_candidate/submission.zip`

| # | 检查项 | 结果 |
|---|---|---|
| 1 | ZIP 文件存在 | ✅ **PASS** |
| 2 | ZIP 可正常解压/读取 | ✅ **PASS**（条目 1000） |
| 3 | 文件数量 = 1000 | ✅ **PASS**（实际 1000） |
| 4 | CRC 全部 PASS | ✅ **PASS**（`testzip()` 无损坏） |
| 5 | TXT 格式正确 | ✅ **PASS**（非法 0 行 / 共 10212 行） |
| 6 | **不存在空文件** | ❌ **FAIL —— 存在 2 个空文件** |
| 7 | 类别 ID 合法 | ✅ **PASS**（越界 0，非整数 0） |
| 8 | confidence 合法 | ✅ **PASS**（>1: 0，<0: 0；范围 [0.001000, 0.981424]） |
| 9 | 坐标格式合法 | ✅ **PASS**（越界 0，框超图像边界 0） |
| 10 | 退化框检查（不修改代码） | ⚠️ **226 行**（原样记录，**未修改 `predict_rect.py`**） |
| 11 | ZIP SHA256 | ✅ 已记录（见 §2） |
| 12 | 与已审计版本一致 | ✅ **PASS**（SHA256 完全一致） |

**附加检查**：目录条目 0 / 非 `.txt` 文件 0 / 路径前缀异常 0（全部为 `results/<name>.txt`）；
重复文件名 0 / 缺失图片 0 / 多余结果 0；zip 内 stems 与测试源图 1000 张逐一对应；NaN/Inf 0。

### 1.6 关于「空文件」FAIL 的说明（重要）

检出的 **2 个空 TXT** 是**冻结 pipeline 的既定行为**，非缺陷：

- `scripts/predict.py` 明确约定：「无检测 → 空 TXT（**文件必须存在**）」——
  空文件表示该图无任何检测目标，比赛要求每张图都要有同名文件。
- **对照证据**：正式提交包 `submissions/rgbird_ir_quicktest/submission.zip`
  （即线上已获 **0.53180** 的那一份）**含 6 个空文件**，已被官方评分接受。

> 因此该项判为 **"与既有提交约定一致、不构成阻塞"**，但按你的检查清单**如实标记为 FAIL**，不做隐藏。
> 未对任何文件做修改。

### 1.10 关于退化框（226 行）

与正式提交包（155 行）同源，均来自冻结 `predict.py::_format_lines` 对边界外框不设最小边长。
conf 均值 0.0027、最大 0.047，排在置信度末尾。**原样记录，未修改任何代码。**

---

## 2. 阶段 2：正式保底保护核验

全部重新计算 SHA256，与冻结清单逐一比对：

| 对象 | 状态 |
|---|---|
| RGBID best.pt（正式保底 0.53180） | ✅ **未变动** |
| F4 best.pt（备用 0.51051） | ✅ **未变动** |
| RGBID 正式 submission.zip | ✅ **未变动** |
| 冻结 `scripts/predict_rect.py` | ✅ **未变动** |
| `scripts/predict.py` | ✅ **未变动** |
| Ensemble 候选 zip（待提交） | ✅ **未变动** |

- 历史实验目录：**47 个，未删除任何目录**
- `reports/` 文件：23 个，未删除任何报告
- 未创建任何新实验目录（无 G7/G8/G9）
- 无残留 python 进程

**未发生**：Ensemble 包覆盖 RGBID 包、RGBID 正式提交被覆盖、任何冻结产物被修改。

---

## 3. 阶段 3：提交未执行的原因（必须澄清）

### 3.1 环境事实

| 核验项 | 结果 |
|---|---|
| 你指定的项目路径 `/root/urban_multimodal_detection` | ❌ **不存在**（本机为 Windows） |
| 本会话实际工作目录 | `d:/gyt/AIC/urban_multimodal_detection` |
| 仓库内是否存在提交/上传通道（脚本 / API / 凭据） | ❌ **不存在** |

### 3.2 结论

**本会话没有任何可用的官方提交通道。** 本机为 Windows 本地环境，
既无比赛平台登录凭据，也无上传脚本或 API；仓库内亦无相关代码。

**历史上所有线上成绩（0.53180 / 0.51051）均由你提供** ——
本会话从未、也无法直接向比赛平台提交或读取成绩。

因此：

- ❌ **未执行提交**
- ❌ **未上传任何文件**
- ❌ **未修改任何 ZIP**
- ✅ 候选包**原封未动**，SHA256 可校验

> ⚠️ 状态标签使用 `ONLINE_SCORE_PENDING`，但**原因是「提交未执行」**，
> 而非「已提交、等待官方每小时返回」。两者含义完全不同，特此澄清，不做混淆。

---

## 4. 阶段 4：提交记录

| 项 | 值 |
|---|---|
| **提交时间** | **未提交**（N/A） |
| **ZIP SHA256** | `cee8e90205abeefc2b8593c5ff30dbf49634a4bcfa212b599978274d0ac75d97` |
| **ZIP 文件大小** | **340,634 字节**（332.6 KiB） |
| **文件数量** | **1000** |
| **CRC 检查结果** | **PASS**（`zipfile.testzip()` 返回 None） |
| **使用的候选包路径** | `submissions/rgbid_f4_ensemble_3to1_candidate/submission.zip` |
| **对照组** | RGBID Early Fusion（`runs/urban_multimodal_det_yolo11_rgbird_ir_quicktest/weights/best.pt`） |
| **对照组线上成绩** | **0.53180** |
| **实验组** | RGBID + F4 3:1 Ensemble |
| **离线官方公式成绩** | **0.51496** |
| **离线相对差值** | **+0.00568** |
| **官方线上返回成绩** | **`PENDING`** |
| **官方评分返回时间** | N/A |
| **最终线上 Δ** | **无法计算**（`Ensemble线上分数 − 0.53180`，缺被减数） |

### 4.1 与已审计版本的一致性

| 来源 | SHA256 |
|---|---|
| `reports/rgbid_f4_ensemble_3to1_final_audit.md` 记录 | `cee8e90205abeefc2b8593c5ff30dbf49634a4bcfa212b599978274d0ac75d97` |
| 当前 ZIP 实测 | `cee8e90205abeefc2b8593c5ff30dbf49634a4bcfa212b599978274d0ac75d97` |
| **判定** | ✅ **完全一致 —— 无需 `BLOCKED_PACKAGE_MISMATCH`** |

---

## 5. 阶段 5：结果判定

> # `ONLINE_SCORE_PENDING`
>
> **原因：提交未执行（本机无官方提交通道）**，因此没有官方线上成绩可供判定。
> **不自行推测结果。**

依据你的判定规则：

| 条件 | 是否适用 |
|---|---|
| `Ensemble线上 > 0.53180` → `ONLINE_IMPROVEMENT_CONFIRMED` | ❌ 无线上成绩 |
| `Ensemble线上 ≈ 0.53180` → `NO_MEANINGFUL_ONLINE_GAIN` | ❌ 无线上成绩 |
| `Ensemble线上 < 0.53180` → `ONLINE_GAIN_REJECTED` | ❌ 无线上成绩 |
| 官方评分尚未返回 → **`ONLINE_SCORE_PENDING`** | ✅ **适用** |

**RGBID 仍为正式保底方案，线上 0.53180 不变。** Ensemble 仍为离线研究候选。

---

## 6. 交付给你的人工提交步骤（包已就绪、可直接提交）

候选包已通过全部提交前检查（除空文件项，见 §1.6，与既有约定一致），**无需任何重新生成**：

1. 取文件：`submissions/rgbid_f4_ensemble_3to1_candidate/submission.zip`
2. **提交前先校验 SHA256**（应为 `cee8e902…c75d97`，见 §4）——确保与已审计版本一致
3. 上传至比赛平台
4. 等官方成绩返回（你说明为每小时一次）
5. 把返回成绩回填到本报告 §4 的 `PENDING` 处，并按 §5 规则判定

**云端另一处注意**：你提到的 `/root/urban_multimodal_detection` 若是 AutoDL 实例，
该实例上也需要同步本候选包（本机与云端不在同一文件系统）。

---

## 7. 本次未执行的动作（合规记录）

未训练 / 未 CPU 缩水实验 / 未修改模型结构 / 未修改 loss / 未修改 batch / 未修改 imgsz /
未修改 `predict_rect.py` / 未修改 NMS / 未修改 confidence / 未修改 max_det /
未修改 Ensemble 权重 / **未重新生成 submission.zip** / 未删除历史实验 /
**未覆盖 RGBID 正式提交** / 未自动连续提交多个版本 / 未上传 / 未提交。

**本文件是本阶段唯一新增的产物。**

---

## 8. 下一步建议

1. **由你或云端环境执行 §6 的提交**（本机无法代为执行）；
2. 提交前务必用 §4 的 SHA256 校验候选包，避免提交到错误版本；
3. 成绩返回后回填并判定；
4. **在成绩返回前不要继续大量提交**（按你的规则）；
5. 无论结果如何，**RGBID 保底包与正式提交包一律保留，不删除**。

### 预期不确定性的诚实提示

即使提交后拿到成绩，也请留意：

- 离线**官方口径**为 +0.00568，但离线 **fork 口径为 −0.01278**（符号相反，且阈值鲁棒）；
- Ensemble 用 **+2100 FP** 换 **+108 TP**，Precision 由 0.83887 降至 0.79877；
- 单次线上成绩受测试集采样影响，±0.01 量级的差异不一定稳定可复现。

**本次 A/B 的价值正在于用真实线上成绩消除上述口径分歧 —— 这正是它值得做的理由。**
